#posts
  - @posts.each do |post|
    .post
      %h2.title= link_to post.title, post
      %p.date
        This post was created
        = time_ago_in_words(post.created_at)
        ago by
        = post.user.email
